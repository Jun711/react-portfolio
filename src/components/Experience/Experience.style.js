import styled from 'styled-components';
import { FixedWidthContainer, Wrapper } from '../../theme/grid';

export const ExperienceWrapper = styled(Wrapper)``;

export const ExperienceContainer = styled(FixedWidthContainer)`  
  text-align: left;
  h3 {
    text-align: left;
  }
`;
